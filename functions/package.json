{
  "name": "functions",
  "description": "Firebase Cloud Functions for Farm Collective UK",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.3",
    "stripe": "^14.10.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "typescript": "^5.3.3"
  },
  "private": true
}
